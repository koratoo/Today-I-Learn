
`char *p = "KOREA"`에서 `printf("%s\n", p+3)`를 호출하면, `p`가 가리키는 주소로부터 3번째 위치의 문자부터 끝까지 출력하게 됩니다.

즉, "KOREA"라는 문자열에서 'E'가 4번째 위치에 있으므로 (C 언어에서 인덱스는 0부터 시작합니다), 'E'부터 끝까지인 "EA"가 출력됩니다. 

따라서 화면에는 "EA"라는 문자열이 나타나게 됩니다. `\n`은 줄바꿈(newline)을 의미하므로, 이 문자열 뒤에 출력되는 내용은 새로운 줄에서 시작하게 됩니다.

