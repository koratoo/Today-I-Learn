1. SELECT절 서브쿼리

SELECT절의 서브쿼리는 하나의 열처럼 사용한다.

SELECT절의 서브쿼리는 데이터의 양이 많을수록 실행속도가 느려지기 때문에 거의 사용되지 않는 명령어이다.


2. FROM절의 서브쿼리

FROM절에 사용되는 서브쿼리는 하나의 테이블처럼 사용된다.
테이블 처럼 사용되므로, 열 이름과 테이블 명을 꼭 명시해줘야 한다.


3.WHERE절의 서브쿼리
